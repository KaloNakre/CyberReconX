# Java Starter

This folder contains a minimal Java entrypoint `CyberReconX.java` added as requested.

Compile and run on Linux:

```bash
javac CyberReconX.java
java CyberReconX
```

This is intentionally minimal to avoid changing the main Python project behavior.
